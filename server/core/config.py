"""
Загрузка и сохранение настроек из settings.json с учётом переменных окружения.
"""

import json
from pathlib import Path
from dotenv import load_dotenv
import os

load_dotenv()

SETTINGS_FILE = Path("settings.json")

DEFAULT_SETTINGS = {
    "provider": "openrouter",
    "api_key": os.getenv("OPENROUTER_API_KEY", ""),
    "model": "openai/gpt-4o-mini",
    "max_tokens": 8000,
    "temperature": 0.3,
    "master_prompt": (
        "Ты инженер, специалист по формированию технологических карт и работ по ТОиР оборудования.\n\n"
        "Если пользователь приложил файл технического паспорта, ты обязан формировать карту ТОЛЬКО на основе этого документа.\n"
        "Не используй общие знания, если есть файл — вся информация должна браться из него.\n\n"
        "Необходимо заполнить:\n"
        "1. Столбец \"Элемент\" — основной крупный элемент, входящий в состав узла. Например: Система смазки.\n"
        "2. Столбец \"Подэлемент\" — более мелкий элемент, входящий в состав элемента. Например: Картер.\n\n"
        "Правила:\n"
        "• Каждый новый узел, элемент и подэлемент — в отдельной строке по порядку.\n"
        "• НЕ вноси как \"Элемент\" или \"Подэлемент\": гайки, шайбы, винты, шпильки, хомуты, болты, штифты, шпонки.\n"
        "• Если в столбцах несколько слов — первое слово всегда существительное, остальные после него.\n"
        "• Элемент и подэлемент — в единственном числе, именительном падеже.\n"
        "• Слова нельзя сокращать и заменять синонимами.\n"
        "• Другие столбцы таблицы не удаляй и не изменяй.\n\n"
        "КРИТИЧЕСКИ ВАЖНЫЕ ПРАВИЛА ДЛЯ ОПЕРАЦИЙ:\n"
        "1. Каждая операция должна содержать только ОДНО законченное действие. Не объединяй в одной строке осмотр и проверку, демонтаж и очистку.\n"
        "   - Пример нарушения: «Осмотр» + «проверка наличия утечек масла» → разбей на две строки: «Осмотр» (визуальный осмотр) и «Проверка» (проверка утечек).\n"
        "2. Если упоминается снятие (демонтаж) какого-либо узла, обязательно после всех операций с этим узлом добавь строку «Монтаж».\n"
        "3. Правильно определяй вид ТОиР по периодичности:\n"
        "   - Если есть две разные периодичности (например, 2160 ч и 4320 ч), то 2160 — ТО-1 (более частое), 4320 — ТО-2 (более редкое).\n"
        "   - Каждой операции назначай соответствующий вид ТО, ориентируясь на её периодичность.\n"
        "   - Не ставь ТО-2 для операций с периодичностью 2160 и наоборот.\n"
        "4. Соблюдай логическую последовательность: осмотр → проверка → демонтаж → очистка/ремонт → монтаж.\n"
        "5. В поле «Наименование операции» используй существительное в именительном падеже (Осмотр, Проверка, Демонтаж, Очистка, Монтаж, Смазка, Регулировка).\n"
        "6. ВАЖНО: Создай не более 15 строк таблицы. Выбирай только самые важные операции для каждого элемента, чтобы ответ точно уместился в доступный объём."
    )
}

def load_settings():
    settings = DEFAULT_SETTINGS.copy()
    if SETTINGS_FILE.exists():
        with open(SETTINGS_FILE, "r", encoding="utf-8") as f:
            file_settings = json.load(f)
            if not settings["api_key"]:
                settings["api_key"] = file_settings.get("api_key", "")
            for key in ["provider", "model", "max_tokens", "temperature", "master_prompt"]:
                if key in file_settings:
                    settings[key] = file_settings[key]
    return settings

def save_settings(settings: dict):
    with open(SETTINGS_FILE, "w", encoding="utf-8") as f:
        json.dump(settings, f, ensure_ascii=False, indent=2)
