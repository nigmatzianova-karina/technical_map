# 📋 TK AI Generator — Техническое описание API и механизмы интеграции

> **Версия документа:** 1.0  
> **Дата:** Апрель 2026  
> **Назначение:** Документ описывает способы подключения внешних приложений к системе генерации технологических карт ТОиР оборудования.

---

## 🔹 1. Общая информация

**TK AI Generator** — веб-приложение на FastAPI для автоматизированного формирования технологических карт технического обслуживания и ремонта (ТОиР) с использованием языковых моделей (OpenAI, OpenRouter).

### Архитектура
```
┌────────────────────┐    ┌─────────────────┐   ┌─────────────────┐
│  Внешнее приложение│──▶│ TK AI Generator │──▶│ LLM-провайдер  │
│  (клиент)          │◀──│ (FastAPI, 8000) │◀──│ (OpenAI/Router)│
└────────────────────┘    └─────────────────┘   └─────────────────┘
                                │
                                ▼
                     ┌─────────────────────┐
                     │  Файловое хранилище │
                     │  (settings.json,    │
                     │   временные файлы)  │
                     └─────────────────────┘
```

### Базовые параметры
| Параметр | Значение |
|----------|----------|
| **Базовый URL** | `http://localhost:8000` (по умолчанию) |
| **Протокол** | HTTP/1.1, HTTPS (при настройке reverse proxy) |
| **Формат данных** | JSON, multipart/form-data |
| **Кодировка** | UTF-8 |

---

## 🔹 2. Аутентификация и авторизация

На текущий момент **аутентификация не реализована**. Все эндпоинты доступны публично.

### 🔐 Рекомендации для продакшена:
```python
# Пример добавления API-key аутентификации
from fastapi import Security, HTTPException
from fastapi.security import APIKeyHeader

API_KEY_HEADER = APIKeyHeader(name="X-API-Key", auto_error=False)

async def verify_api_key(api_key: str = Security(API_KEY_HEADER)):
    if api_key != os.getenv("EXPECTED_API_KEY"):
        raise HTTPException(status_code=403, detail="Invalid API key")
    return api_key

# Использование в роуте:
@app.post("/api/chat", dependencies=[Depends(verify_api_key)])
async def chat_endpoint(...):
    ...
```

---

## 🔹 3. Описание эндпоинтов

### 3.1 `GET /` — Веб-интерфейс
Возвращает HTML-страницу основного интерфейса.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| — | — | — | — |

**Ответ:** `text/html`

---

### 3.2 `GET /api/settings` — Получение настроек
Возвращает текущую конфигурацию подключения к LLM.

**Ответ (200 OK):**
```json
{
  "provider": "openrouter",
  "api_key": "sk-or-***",
  "model": "anthropic/claude-3.5-sonnet",
  "max_tokens": 3000,
  "master_prompt": "Ты инженер, специалист по формированию..."
}
```

---

### 3.3 `POST /api/settings` — Обновление настроек
Обновляет конфигурацию приложения (сохраняет в `settings.json`).

**Запрос (application/json):**
```json
{
  "provider": "openrouter",
  "api_key": "ваш_ключ",
  "model": "anthropic/claude-3.5-sonnet",
  "max_tokens": 4000,
  "master_prompt": "Кастомный системный промпт..."
}
```

| Поле | Тип | Описание |
|------|-----|----------|
| `provider` | string | `"openrouter"` или `"openai"` |
| `api_key` | string | Ключ доступа к LLM-провайдеру |
| `model` | string | Идентификатор модели (зависит от провайдера) |
| `max_tokens` | integer | Лимит токенов в ответе (500–16000) |
| `master_prompt` | string | Системный промпт для ИИ |

**Ответ (200 OK):**
```json
{ "status": "ok" }
```

---

### 3.4 `POST /api/chat` — 🔑 Основной эндпоинт генерации
Генерирует технологическую карту на основе параметров оборудования и опционального технического паспорта.

**Content-Type:** `multipart/form-data`

| Поле | Тип | Обязательный | Описание |
|------|-----|--------------|----------|
| `message` | string | ✅ | Текстовый запрос/инструкция для ИИ |
| `model_name` | string | ✅ | Название модели оборудования (напр. `"БелАЗ 7547"`) |
| `equipment_class` | string | ❌ | Класс оборудования (напр. `"Самосвалы"`) |
| `subclass` | string | ❌ | Подкласс (напр. `"Карьерные"`) |
| `file` | file | ❌ | Технический паспорт: `.pdf`, `.docx`, `.txt`, `.csv` |

**Пример запроса (curl):**
```bash
curl -X POST http://localhost:8000/api/chat \
  -F "message=Сгенерируй карту ТО для двигателя" \
  -F "model_name=БелАЗ 7547" \
  -F "equipment_class=Самосвалы" \
  -F "subclass=Карьерные" \
  -F "file=@/path/to/manual.pdf"
```

**Ответ (200 OK):**
```json
{
  "text": "Краткое текстовое описание результата...",
  "table_rows": [
    [
      "Система смазки",
      "Картер",
      "Осмотр",
      "Визуальный осмотр картера...",
      "ТО-1",
      "4320",
      "2.0",
      "1",
      "Слесарь по ремонту автомобилей, 3 разряд",
      "2.0",
      "",
      "",
      "",
      "",
      "Каска защитная, 1 шт; Очки защитные, 1 шт",
      "Затормозить технику; Выполнять работы при неработающем двигателе"
    ]
  ],
  "xlsx_file": "UEsDBBQABgAIAAAAIQD...[base64-encoded XLSX]...==",
  "xlsx_filename": "ТК_БелАЗ 7547_Самосвалы.xlsx"
}
```

| Поле | Тип | Описание |
|------|-----|----------|
| `text` | string | Текстовое резюме от ИИ |
| `table_rows` | array[] | Массив строк таблицы (16 колонок) |
| `xlsx_file` | string (base64) | Готовый XLSX-файл в кодировке base64 |
| `xlsx_filename` | string | Рекомендуемое имя файла для скачивания |

> ⚠️ **Важно:** Поле `xlsx_file` может быть `null`, если генерация таблицы не удалась.

---

### 3.5 `GET /api/table_template` — Шаблон таблицы
Возвращает заголовки столбцов для построения UI.

**Ответ (200 OK):**
```json
{
  "headers": [
    "Класс", "Подкласс", "Нормализованный код модели",
    "Элемент", "Подэлемент", "Наименование операции",
    "Краткое содержание работ", "Вид ТОиР", "Периодичность",
    "Норма времени, часов", "Количество исполнителей",
    "Профессия/Квалификация", "Трудоёмкость, человеко/часов",
    "Наименование ТМЦ", "Количество ТМЦ", "Единицы измерения ТМЦ",
    "Наименование инструменты", "Средства индивидуальной защиты",
    "Требования по безопасности"
  ]
}
```

---

## 🔹 4. Структура данных таблицы

Все строки таблицы должны содержать **16 значений** в следующем порядке:

| № | Столбец | Описание | Пример |
|---|---------|----------|--------|
| 1 | Элемент | Крупный узел | `Система смазки` |
| 2 | Подэлемент | Деталь узла | `Картер` |
| 3 | Наименование операции | Тип работы | `Осмотр` |
| 4 | Краткое содержание работ | Описание | `Визуальный осмотр на трещины` |
| 5 | Вид ТОиР | Тип обслуживания | `ТО-1`, `ТО-2`, `ТР` |
| 6 | Периодичность | Интервал в моточасах | `4320` |
| 7 | Норма времени, часов | Длительность операции | `2.0` |
| 8 | Количество исполнителей | Число работников | `1` |
| 9 | Профессия/Квалификация | Требования к персоналу | `Слесарь, 3 разряд` |
| 10 | Трудоёмкость, чел/час | Расчётное значение | `2.0` |
| 11 | Наименование ТМЦ | Материалы | *(пусто)* |
| 12 | Количество ТМЦ | Объём материалов | *(пусто)* |
| 13 | Единицы измерения ТМЦ | Ед. измерения | *(пусто)* |
| 14 | Наименование инструменты | Инструмент | *(пусто)* |
| 15 | Средства индивидуальной защиты | СИЗ | `Каска; Очки; Перчатки` |
| 16 | Требования по безопасности | Инструкции | `Затормозить технику` |

> 📌 Пустые значения обозначаются пустой строкой `""`. Разделитель в сыром ответе ИИ — символ `|`.

---

## 🔹 5. Механизмы интеграции

### 🔄 Вариант 1: Прямые HTTP-запросы (REST)
Подходит для скриптов, микросервисов, backend-приложений.

**Пример на Python:**
```python
import requests
import base64

def generate_tk(model_name: str, equipment_class: str, message: str, file_path: str = None):
    url = "http://localhost:8000/api/chat"
    files = {}
    if file_path:
        files = {"file": open(file_path, "rb")}
    
    data = {
        "message": message,
        "model_name": model_name,
        "equipment_class": equipment_class,
        "subclass": "Карьерные"
    }
    
    response = requests.post(url, data=data, files=files)
    result = response.json()
    
    # Сохранение XLSX
    if result.get("xlsx_file"):
        with open(result["xlsx_filename"], "wb") as f:
            f.write(base64.b64decode(result["xlsx_file"]))
    
    return result
```

### 🌐 Вариант 2: Интеграция через веб-интерфейс (iframe)
Встраивание UI в корпоративный портал:
```html
<iframe 
  src="http://tk-ai.local:8000" 
  width="100%" 
  height="800px"
  style="border: none; border-radius: 12px;">
</iframe>
```

### ⚙️ Вариант 3: WebSocket (перспектива)
На текущий момент не реализован, но может быть добавлен для стриминга ответа ИИ в реальном времени.

### 🔗 Вариант 4: Webhook-уведомления (перспектива)
Возможность отправки результата на внешний URL после генерации:
```json
{
  "webhook_url": "https://your-system/api/tk-received",
  "callback_token": "secret"
}
```

---

## 🔹 6. Обработка ошибок

| Код | Описание | Действия |
|-----|----------|----------|
| `400` | Некорректные параметры / отсутствующий API-ключ | Проверить обязательные поля и настройки |
| `413` | Файл превышает лимит размера | Уменьшить размер файла (<10 МБ рекомендуется) |
| `422` | Ошибка валидации multipart-формы | Проверить названия полей и Content-Type |
| `500` | Ошибка провайдера LLM / внутренняя ошибка | Проверить логи, повторить запрос с экспоненциальной задержкой |
| `504` | Таймаут ответа от ИИ | Увеличить `max_tokens` или выбрать более быструю модель |

**Пример ответа с ошибкой:**
```json
{
  "detail": "API ключ не установлен в настройках"
}
```

---

## 🔹 7. Рекомендации по развёртыванию

### 🐳 Docker (пример `Dockerfile`)
```dockerfile
FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 📦 requirements.txt (минимальный набор)
```
fastapi==0.109.0
uvicorn[standard]==0.27.0
openai>=1.10.0
httpx>=0.26.0
openpyxl>=3.1.0
PyPDF2>=3.0.0
python-docx>=1.1.0
python-multipart>=0.0.6
```

### ⚙️ Переменные окружения (рекомендуется)
```bash
# .env
API_KEY_OR=sk-or-v1-...
API_KEY_OAI=sk-proj-...
MAX_FILE_SIZE_MB=10
CORS_ORIGINS=["https://your-domain.com"]
```

### 🔐 Безопасность
- Не хранить API-ключи в `settings.json` в продакшене — использовать env-переменные
- Настроить CORS через `CORSMiddleware`
- Ограничить размер загружаемых файлов:
```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=["https://your-domain.com"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

---

## 🔹 8. Примеры использования

### 📊 Интеграция с 1С:Предприятие (через HTTP-запрос)
```bsl
// 1C Enterprise Script
Процедура СгенерироватьТК(Модель, Класс, ФайлПаспорта)
    
    Запрос = Новый HTTPЗапрос("http://tk-ai.local:8000/api/chat");
    Заголовки = Запрос.Заголовки;
    Заголовки.Вставить("Content-Type", "multipart/form-data");
    
    Тело = Новый ЗаписьДанныхВПамяти();
    // ... формирование multipart ...
    
    Ответ = HTTPСоединение.ОтправитьДляОбработки(Запрос);
    Результат = ПрочитатьJSON(Ответ.ПолучитьТелоКакСтроку());
    
    Если Результат.xlsx_file <> Неопределено Тогда
        ДанныеФайла = Base64СтрокаВДвоичныеДанные(Результат.xlsx_file);
        НовыйФайл = Новый Файл(Результат.xlsx_filename);
        ДанныеФайла.Записать(НовыйФайл.ПолноеИмя);
    КонецЕсли;
    
КонецПроцедуры
```

### 🤖 Интеграция с чат-ботом (Telegram)
```python
# telebot + TK AI Generator
@bot.message_handler(commands=['tk'])
def handle_tk_request(message):
    # Парсим: /tk БелАЗ7547 "Сгенерируй карту для двигателя"
    result = generate_tk(
        model_name="БелАЗ 7547",
        equipment_class="Самосвалы", 
        message="Сгенерируй карту для двигателя"
    )
    
    if result.get("xlsx_file"):
        with open("temp.xlsx", "wb") as f:
            f.write(base64.b64decode(result["xlsx_file"]))
        bot.send_document(message.chat.id, open("temp.xlsx", "rb"))
        bot.reply_to(message, result["text"])
```

---

## 🔹 9. Чеклист перед интеграцией

- [ ] Убедиться, что `main.py` запущен и доступен по сети
- [ ] Настроить `settings.json` с валидным API-ключом
- [ ] Проверить доступность эндпоинта `GET /api/settings`
- [ ] Протестировать `POST /api/chat` с минимальными параметрами
- [ ] Обработать base64-декодирование `xlsx_file` на стороне клиента
- [ ] Реализовать retry-логику для ошибок 5xx
- [ ] Настроить логирование запросов для аудита

---

## 🔹 10. Контакты и поддержка

| Роль | Контакт |
|------|---------|
| Техническая документация | `docs@your-company.local` |
| Интеграционные вопросы | `api-support@your-company.local` |
| GitHub-репозиторий | `https://github.com/your-org/tk-ai-generator` |

---